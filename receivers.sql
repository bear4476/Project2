create table receivers(
 id varchar(20),
 session tinytext,
 receiver varchar(20),
 phone varchar(30),
 address varchar(100),
 message text,
 buydate varchar(30),
 sender varchar(20),
 ordernum varchar(20),
 status int(1));